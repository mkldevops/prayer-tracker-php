{{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form.name) }}
    </div>
    <button class="btn btn-lg btn-block btn-info"><i class="fas fa-save"></i> {{ button_label|default('Save'|trans) }}</button>
{{ form_end(form) }}
