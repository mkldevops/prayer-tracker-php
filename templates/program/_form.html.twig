{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.dayObjective) }}
    <button class="btn btn-lg btn-block btn-info my-4"><i class="fas fa-save"></i> {{ button_label|default('Save'|trans) }}</button>
{{ form_end(form) }}
