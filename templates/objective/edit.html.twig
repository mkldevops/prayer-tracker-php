{% extends 'base.html.twig' %}

{% block title %}Edit Objective{% endblock %}

{% block body %}
    <div class="row text-center">
        <div class="col-1 h1">
            <a href="{{ path('objective_show', {'id': objective.id}) }}"><i class="fas fa-caret-left"></i></a>
        </div>
        <h1 class="col-10">{{ 'Edit Objective "%name%"'|trans({'%name%' : objective }) }}</h1>
        <div class="col-1">
            <div><a href="{{ path('objective_show', {'id': objective.id}) }}"><i class="fas fa-file"></i></a></div>
            {{ include('objective/_delete_form.html.twig') }}
        </div>
    </div>

    {{ include('objective/_form.html.twig', {'button_label': 'Updat÷e'|trans }) }}
{% endblock %}
